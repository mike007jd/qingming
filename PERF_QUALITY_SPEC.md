# 1080p / 30 fps 性能规格

2026-09-12。用户批准 Auto 几何与画质取舍；本规格替代旧 T0–T9 的 Auto 高模像素一致要求。旧规格随修改前源码保存在 `backup/pre-30fps-20260912.tar.gz`。Cinema 保留原始高模、材质、4×MSAA 和 4096 阴影图。

## 目标

当前 M2 Max、原生 Chrome、真实 1920×1080 渲染尺寸，正常动画、行走与追舟平均 ≥30 fps，帧间隔 P95 ≤40ms。三画风 × 十镜头，以及每种画风的行走/追舟，各预热后采样，两轮独立浏览器运行。统计正常应用循环；测量帧不插入 `gl.finish`、同步截图、GPU 查询。另行采集浏览器帧轨迹核对呈现和队列积压。相对提速不能代替验收。

## Auto 资产与渲染

| 对象 | 近 / 中 / 远三角上限 | 投影直径阈值 |
| --- | --- | --- |
| NPC，每人 | 3000 / 1000 / 300 | 180 / 60px |
| 树木，整株 | 8000 / 2000 / 300 | 160 / 48px |

- 保留 584 人、成人/儿童 31 关节及原动作、分布、舟车生态、碰撞、船员绑定。NPC 两个共享材质、≤1K 图集，离散关节编号，每顶点最多四个归一化权重；权重/法线/颜色从原表面投影，位置与法线共享蒙皮矩阵。
- LOD 保留 20% 回切缓冲；NPC 小于 3px 不提交，大于 5px 恢复。包围体覆盖全部动作关键帧。
- 树冠使用源叶片烘焙簇卡片，中远档实际减少叶簇。建筑按独立构件减面，保留每个构件及其体积；会压扁构件的结果退回该构件原几何，结构预算允许超出，远档不增加面数。瓦片近景保留完整原几何，中远景使用连续封闭屋面，按整屋顶400/120px阈值切换。
- 虹桥护栏、桥面、桥拱与桥台保留完整原始几何。
- 同材质静态物体使用 Three.js 原生 BatchedMesh，保留逐物体/逐空间块剔除。路面、地形按约 32m 分块并保留完整表面。水面几何 1024×64，按约 32m 分块；水场仍 2048×256、1/120 秒子步，物理取样不降分辨率。
- Auto 2×MSAA / 1024 阴影图；普通纹理维持 ≤1K。
- 主、倒影、阴影分别维护可见集合。后两者粗一档；NPC 阴影进入距离 40m、移出 44m。沿用固定镜头动画隔帧刷新、相机/设置变化立即刷新规则。
- 描边继续独立渲染，使用主画面性能网格和可见集合。沿用 PCFSoft 与原始连续阴影投影。
- 100% 画卷强度时省略被画卷颜色覆盖的 PBR 光照结果；原材质、混合强度与 Cinema 使用完整光照。100%画卷隐藏全部无贡献点光源，其余模式保留固定六个光源槽位，以免切镜头生成新着色器变体。材质按实际着色代码共享程序，并在加载阶段调用原生compileAsync。
- 门碰撞接入原 4m 空间网格，按完整开合范围和 0.6m 人物半径保守插入；更大查询退回全扫描，精确碰撞保持原实现。

| 指标 | Auto 指导预算 |
| --- | --- |
| 主画面三角 | ≤150 万 |
| 全通道平均三角 | ≤500 万/帧 |
| 完整刷新三角 | ≤800 万 |
| 全通道平均调用 | ≤1200/帧 |

最终按实际帧率验收；原生 multi-draw 的调用计数不等价于 GPU 工作量。

## 构建与回退

接口值仍为 `auto` / `cinema`，UI 明确性能档。`autoLodDraws` 是资产清单的可选性能几何；原始 draw 范围独立保存，Cinema 不使用派生范围。`autoSource` 标记原始数据尾界，以便派生构建可重复运行。

`npm run build:assets` 只从当前权威 runtime/GLB 生成 Auto 几何、SoA 和清单。`-- --rebuild-scene` 是显式从制作输入重新制作布局/Blender 的入口；本轮未运行该路径。原始四个 GLB 与四份 Blender 高模保留。

修改前快照：`backup/pre-30fps-20260912.tar.gz`。阶段清单：`production/auto-lod-manifest.json`、`production/auto-detail-manifest.json`。回退整体还原快照中的源码与生成资产；不要只还原其中一侧。

## 验收证据

- `npm test`：既有行为及新门网格、质量隔离、批次契约。
- `npm run test:auto-assets`：面数、材质数、LOD 递减、索引、坐标、权重、关节、完整地面分块、虹桥保留。
- `npm run test:package`：GLB/Blender/纹理清单、SoA 和服务打包检查。
- `?verify=1`：42 项 GPU、水场、进出建筑、桥面通行及三画风/十镜头检查。
- 另验证暂停/继续、质量切换、缩放、4K PNG、1080p 十秒录像、所有动作与船员绑定、视锥边缘/快速转向/追舟。
- 视觉允许脸部、衣褶、配件和中远细节减少；保留轮廓、入口、树冠、身份颜色与动作可读性。检查破面、骨架塌陷、闪烁和缺失。Auto/Cinema 像素差异仅作描述，不以旧阈值否决批准的画质取舍。
- 结果必须分别报告功能、资产、视觉与绝对帧率；任何未完成/未通过项明确列出。

切镜头和建筑破面修复见 [修复记录](evidence/landmarks-switch-20260912.md)。

此前阶段结果见 [性能改造记录](evidence/auto-30fps-20260912.md)；最新结构修复与预算以修复记录为准。

水体和飞溅共享画风的纸色、墨色、饱和度与强度。Antique Silk 使用绢色水面和断续墨线波纹，保持倒影明暗、泡沫位置、水场和舟船取样。Original Materials 直接输出物理水色。加载阶段冻结生态并预热十镜头的完整通道，避免首切镜头才编译阴影和描边。
